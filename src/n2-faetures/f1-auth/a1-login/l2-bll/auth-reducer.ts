const initialState = {};
type InitialStateType = typeof initialState

export const authReduser = (state: InitialStateType, action: any) => {
    switch (action.type) {
        default:
            return state
    }
};
